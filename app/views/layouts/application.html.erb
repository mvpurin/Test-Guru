<!DOCTYPE html>
<html>
  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="widtg=device-width, initial-scale=1, shrink-to-fit=no">

    <title>TestGuru</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style type="text/css">
      .footer {
        position: fixed;
        left: 0; bottom: 0;
      }
    </style>
  </head>

  <body>
    <%= render 'shared/nav' %>

    <%= flash_message %>

    <%= yield %>

    <div>
    <div class="footer">
      <%= render "layouts/footer" %>
    </div>
  </div>
  </body>

</html>
