<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.size %></td>
  <td><%= link_to t('admin.tests.index.edit'), admin_test_questions_path(test) %></td>
  <td><%= link_to t('admin.tests.index.delete'),
                  admin_test_path(test),
                  method: :delete,
                  data: {confirm: t('confirm')}%>
  </td>
</tr>
