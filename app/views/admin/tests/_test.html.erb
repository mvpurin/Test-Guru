<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.size %></td>
  <td><%= link_to 'Edit', admin_test_questions_path(test) %></td>
  <td><%= link_to 'Delete',
                  admin_test_path(test),
                  method: :delete,
                  data: {confirm: 'Are you sure?'}%>
  </td>
</tr>
